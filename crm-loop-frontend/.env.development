# ESLint webpack plugin (lista enorme de avisos legados no terminal durante npm run start:dev).
# Remova ou comente esta linha se quiser ver todos os avisos do CRA no terminal.
DISABLE_ESLINT_PLUGIN=true
